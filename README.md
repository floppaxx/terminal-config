# Terminal configuration with ansible

1. Change variables in **configure_terminal_playbook.yaml**
2. Run ansible playbook use ``` ansible-playbook configure_terminal_playbook.yaml --ask-become-pass ```